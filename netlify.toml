[build]
  command = "npm run build"
  publish = "dist"

# Redirecciones para asegurar que las rutas se manejen correctamente en Astro
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
